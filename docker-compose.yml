services:
  blog:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - blog-uploads:/app/uploads
      - blog-data:/app/data
    environment:
      - JWT_SECRET=your-random-secret-here
    restart: unless-stopped

volumes:
  blog-uploads:
  blog-data:
